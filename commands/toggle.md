---
description: Toggle claude-cues sounds on or off
allowed-tools: Bash("${CLAUDE_PLUGIN_ROOT}/bin/play.sh":*)
---

Run this exact command and report its one-line output to the user:

```
"${CLAUDE_PLUGIN_ROOT}/bin/play.sh" toggle
```
